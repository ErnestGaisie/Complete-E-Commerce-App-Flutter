import 'package:flutter/material.dart';
import 'components/body.dart';

class PhoneNumberRegScreen extends StatelessWidget {
  static String routeName = "/phone_number_register";

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body(),
    );
  }
}
